import React from "react";
import { Box, Flex, HStack, VStack, InputGroup, Input, IconButton } from "@chakra-ui/react";
import NavBar from "../components/NavBar";

const Home = () => {

  return (
    <Box w='100vw'>
        <NavBar/>
    </Box>
  
  );
};

export default Home;
